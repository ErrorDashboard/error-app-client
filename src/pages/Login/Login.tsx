import LoginCard from './components/LoginCard';

const LoginPage = () => {
  return (
    <div className="w-full flex flex-col justify-center items-center text-center mt-20">
      <LoginCard />
    </div>
  );
};

export default LoginPage;
